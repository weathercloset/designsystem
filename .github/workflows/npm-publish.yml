on:
  push:
    branches:
      - main

jobs:
  publish-npm:
    name: 🚀 publish
    runs-on: ubuntu-latest
    steps:
      - name: 📌 checkout
        uses: actions/checkout@v2.1.1
      - name: 🟢 node
        uses: actions/setup-node@v2https://github.com/weathercloset/designsystem
        with:
          node-version: 16
          registry-url: https://registry.npmjs.org
      - name: 🚀 publish
        run: npm publish --access public
        env:
          NODE_AUTH_TOKEN: ${{secrets.NPM_AUTH_TOKEN}}
